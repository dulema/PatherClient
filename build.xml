<?xml version="1.0" ?>

<project name="PatherClient" default="build-client" basedir=".">
	
        <path id="lib.path">
	        <fileset dir=".">
	           <include name="lib/*.jar" />
	        </fileset>
	</path>

	<target name="clean">
		<delete dir="build/" />
	</target>

	<target name="build-client">
		<mkdir dir="build"/>
		<mkdir dir="build/client"/>
		<javac debug="true" debuglevel="lines,source" srcdir="src/client" destdir="build/client" classpathref="lib.path" />
		<copy todir="build/client">
		    <fileset dir="src/client" includes="**/*.xml,**/*.gram" />
		</copy>
	</target>

	<target name="run" depends="build">
		<java classname="main.Client" classpath="build/" fork="true" classpathref="lib.path" maxmemory="256m" />
	</target>
	
	<target name="sphinx-demo" depends="build">
			<java classname="main.sphinx.HelloWorld" classpath="build/" fork="true" classpathref="lib.path" maxmemory="256m" />
	</target>

</project>
